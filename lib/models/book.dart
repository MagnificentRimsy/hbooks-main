class Book {
  String title,
      author,
      image,
      url,
      description;

  int page, comment;
  double rating;

  Book(this.title, this.author, this.image, this.url,this.page, this.comment, this.rating, this.description);
}


